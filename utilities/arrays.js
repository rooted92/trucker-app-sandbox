let types = ['Dry Van', 'Reefer', 'Flatbed', 'Step Deck', 'Double Drop', 'Lowboy', 'Conestoga', 'Power Only', 'Tanker'];

let trailerStatuses = ['Available', 'In Transit', 'Docked', 'In Service', 'Out of Service', 'In Repair'];

let lengths = [28, 32, 36, 40, 45, 48, 50, 53];

let loadStatuses = ['Loaded', 'Empty'];

let cleanlinessStatuses = ['Clean', 'Dirty', 'N/A'];

let yardStatuses = ['Open', 'Closed']; 

module.exports = { types, trailerStatuses, lengths, loadStatuses, cleanlinessStatuses, yardStatuses };